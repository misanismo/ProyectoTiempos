@model IEnumerable<ProyectoTiempos.Models.NumeroGanador>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Numero.IdNumero)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sorteo.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Numero.IdNumero)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sorteo.Nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdNumeroGanador }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdNumeroGanador }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdNumeroGanador })
        </td>
    </tr>
}

</table>
