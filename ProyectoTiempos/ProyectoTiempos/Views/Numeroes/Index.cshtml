@model IEnumerable<ProyectoTiempos.Models.Numero>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Numeros)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Numeros)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdNumero }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdNumero }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdNumero })
        </td>
    </tr>
}

</table>
